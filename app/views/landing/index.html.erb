<h1>Landing Page</h1>

<% if logged_in? %>
  <h3>Existing Users</h3>
  <ul>
  <% @users.each do |user| %>
    <b><li><%= "#{user.email}" %></li></b>
  <% end %>
  </ul>
<% end %>

<%= button_to "New User", "/register", method: :get %>
<% if logged_in? %>
  <%= button_to "Log Out", logout_path, method: :delete %>
  <% else %>
  <%= button_to "Log In", login_path, method: :get %>
<% end %>